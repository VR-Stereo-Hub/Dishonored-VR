// legacy/legacy_stubs.inc - no-op stand-ins for the retired diagnostics the live
// code still calls, compiled when DVR_WITH_LEGACY is OFF (the default). With it
// ON the real bodies in src/legacy are compiled instead. Generated by
// tools/refactor-phase7.py from the prototypes in src/mod/fwd.h.

static void AimWatchArm(uint8_t* obj) {}
static void AimWatchReport(const char* why) {}
static void CamPovProbe() {}
static void CamPovWiggle() {}
static void CamFovWiggle() {}
static void CamFovHunt() {}
static void CamSeamTick() {}
static void CameraTraceTick() {}
static void SpawnTraceTick(bool triggerEdge) {}
static void RtDriveUpdate() {}
static bool RtdSnapshot(float* R, float* T, int hand, bool weapon) { return false; }
static void GtCommandVM(float pitchDeg, float yawDeg, float rollDeg, float tx, float ty, float tz) {}
static void GtStop(const char* why) {}
static void GtStart() {}
static void GtTick() {}
static void RtdMarkerTick() {}
static void BoneWigBuildMask() {}
static void SbApply(const char* where) {}
static void SbTick() {}
static inline void BoneWigApply() {}
static void DbgProbeTick() {}
static LONG CALLBACK WalkVEH(EXCEPTION_POINTERS* xp) { return 0; }
static void RtdBuildYPR(const float* ypr, float* out) {}
